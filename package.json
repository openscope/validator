{
  "name": "@openscope/validator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node ./index.js -a _mock/ksea.json --fixes --spawnPatterns",
    "test": "nyc mocha 'lib/**/*.spec.js'",
    "test:watch": "nyc mocha -w --reporter dot 'lib/**/*.spec.js'",
    "posttest": "nyc report --reporter=lcov --reporter=cobertura"
  },
  "author": "Nate Geslin, teamtomkins23@gmail.com",
  "license": "MIT",
  "nyc": {
    "exclude": [
      "**/*.spec.js",
      "_mock"
    ]
  },
  "dependencies": {
    "chalk": "^2.4.1",
    "lodash": "^4.17.10",
    "minimist": "^1.2.0",
    "ora": "^2.1.0"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "nyc": "^12.0.2",
    "sinon": "^5.0.10"
  }
}
